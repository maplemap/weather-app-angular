export class Weather {
  constructor(
    public city: string,
    public temp: string,
    public type: string,
    public tempMin: string,
    public tempMax: string,
    public img: string
  ) {}
}
